{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "/bin/sh -c 'LITESTAR_APP=pydotorg.main:app litestar database upgrade --no-prompt && granian --interface asgi pydotorg.main:app --host 0.0.0.0 --port ${PORT:-8000} --workers 1'",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
